プッシュ済のコミットを取り消したい時は
git reset --hard head^でローカルのコミットを削除
git push origin +ブランチ名
プラスをつけると強制プッシュになる。
共有して開発している時などは安易につかったらまずいけどひとまず一人で課題やるにあたっては問題なし。


クエルのフォークした時に練習用にブランチ切ったつもりでいたのにchallenge用の手つかずブランチがなくなっている。
自分の練習ブランチつぶさずにフォーク元から課題用のブランチをを持ってくる方法探し中。。久しぶりにgit。

自分のrepositoryにブランチ作っといて、そこにクエルのブランチの中身をフェッチすればいいのかな。
後で落ち着いてためそ
1 件の返信
チャンネルにも投稿済

takashima-日曜午前  2ヶ月前
できたー。ので一応記録。
やりたかったこと。
既にフォーク済みのリポジトリの、親リポジトリの特定のブランチを丸ごと自分のリモート・ローカルリポジトリに持ってきたい。
(自分のリポジトリに課題用ブランチがなかったので作りたかった。)
やり方
①自分のローカルリポジトリリスト？に親リポジトリを登録。名前をlabotにした。
git remote add labot　git://github.com/labotinc/quelcode-cakephp
(git remote add 任意の名前 親リポジトリのurl)
urlはリポジトリのトップのurlの’https’を’git’に変えればいけた。
urlこれじゃだめだった。httpsでurlそのままとってくるのが正しいらしい。
②git fetch labot
これでローカルのブランチ一覧に親リポジトリのブランチ一覧が出るようになる。
④$ git checkout -b cakephp-challenge labot/cakephp-challenge
(git checkout -b 自分のローカルブランチに付ける名前 親リポジトリの名前/持ってきたいブランチ名)
これで目当てのブランチがローカルに出来上がる。
⑤$ git push origin cakephp-challenge
最後にローカル→リモートにプッシュしてリモートブランチが作成されて完了。
最近gitの動きがだいぶ分かってきた気がする。
まぁ大体ググって真似する流れですが何やってるのかはわかるようになってきたw 
